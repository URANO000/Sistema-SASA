@{
    ViewData["Title"] = "Resultado de la Carga";
}

<div class="container-fluid">
    <div class="card shadow-sm">
        <div class="card-body text-center">

            <i class="bi bi-check-circle-fill text-success fs-1 mb-3"></i>

            <h4>Carga recibida</h4>

            <p class="text-muted mt-2">
                El archivo fue recibido correctamente y se encuentra listo para el proceso de validación.
            </p>

            <div class="alert alert-info mt-4">
                En una implementación completa, el sistema validaría el formato,
                estructura y consistencia de los datos antes de integrarlos.
            </div>

            <div class="d-flex justify-content-center gap-2 mt-4">
                <a asp-action="Validation" class="btn btn-primary">
                    Ir a Validación
                </a>

                <a asp-action="Index" class="btn btn-outline-secondary">
                    Volver al módulo
                </a>
            </div>

        </div>
    </div>
</div>
