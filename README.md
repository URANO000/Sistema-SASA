Frontend for a management system for Sistemas Analíticos S.A.

--In order to install the packages, type the following on your console
(make sure you are cd into SASA)
1. npm install
2. npm install tailwindcss @tailwindcss/vite
3. npm install react-router-dom
4. npm instal primereact
5. npm install primeicons

--To run the server simply write
npm run dev -- --open